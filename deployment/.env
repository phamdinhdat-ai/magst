# Database Configuration
# PostgreSQL connection string
DATABASE_URL=postgresql+asyncpg://datpd:datpd@localhost:5432/gst_agents

# API Server Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=true
API_LOG_LEVEL=info

# Application Settings
GUEST_SESSION_EXPIRY_HOURS=24
GUEST_MAX_INTERACTIONS_PER_SESSION=100

# Load Balancer Settings (Optimized for speed)
LOAD_BALANCER_ENABLED=false  # Disable for faster single-node processing
LOAD_BALANCER_CPU_THRESHOLD=85
LOAD_BALANCER_MEMORY_THRESHOLD=90
LOAD_BALANCER_NODES=localhost:8001,localhost:8002,localhost:8003
LOAD_BALANCER_HEALTH_CHECK_INTERVAL=60
LOAD_BALANCER_COORDINATOR_MODE=false

# Queue Manager Settings (Optimized for speed)
QUEUE_MANAGER_ENABLED=true
QUEUE_MANAGER_MAX_TASKS=5
QUEUE_MANAGER_TIMEOUT=45
QUEUE_MANAGER_POLLING_INTERVAL=0.1  # Faster polling
QUEUE_MANAGER_BACKEND=memory  # Use 'redis' for production

# Priority Settings
GUEST_PRIORITY=0
EMPLOYEE_PRIORITY=1
MANAGER_PRIORITY=2
CUSTOMER_PRIORITY=1
PREMIUM_CUSTOMER_PRIORITY=2

# Vector Store Settings (if using Ollama embeddings)
VECTOR_STORE_BASE_DIR=./vector_stores_data

# Security Settings
SECRET_KEY=your-secret-key-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Development Settings
DEBUG=true
TESTING=false

# Workflow Settings
MAX_WORKFLOW_ITERATIONS=5
WORKFLOW_TIMEOUT_SECONDS=300

# System Monitoring
HEALTH_CHECK_ENABLED=true
HEALTH_CHECK_INTERVAL=60
METRICS_ENABLED=true

# Performance Optimization Settings
WORKFLOW_MAX_ITERATIONS=2  # Reduced from default for faster responses
WORKFLOW_TIMEOUT=8  # 8 second timeout per workflow (reduced from 15)
LLM_RESPONSE_TIMEOUT=5  # 5 second timeout for LLM responses (reduced from 10)
GUEST_WORKFLOW_FAST_MODE=true  # Enable fast mode for guest workflows

# API Performance Settings
API_WORKER_TIMEOUT=12
API_REQUEST_TIMEOUT=15
UVICORN_WORKERS=1  # Single worker for development
FAST_MODE_ENABLED=true  # Skip complex processing for basic queries

# Skip Heavy Processing (for speed)
SKIP_VECTOR_SEARCH=false  # Set to true to skip vector database operations
SIMPLE_RESPONSES_ONLY=false  # Set to true for mock responses
CACHE_RESPONSES=true  # Enable response caching



LLM_PROVIDER=vllm
LLM_MODEL_NAME=SeaLLMs/SeaLLMs-v3-7B-Chat
LLM_REASONING_MODEL_NAME=SeaLLMs/SeaLLMs-v3-7B-Chat
VLLM_API_URL=http://79.117.27.229:22074
API_KEY=



MCP_SERVER_URL=http://192.168.1.60:50051/sse

# # Ollama settings
# LLM_PROVIDER=ollama
# OLLAMA_MODEL_NAME=qwen3:8b
# OLLAMA_API_URL=http://localhost:11434

COMPANY_DB=gst_vdb
GENETIC_DB=genetic_vdb
MEDICAL_DB=medical_vdb
DRUGS_DB=drugs_vdb
PRODUCTS_DB=products_vdb

CHUNK_SIZE=1024
OVERLAP_SIZE=256
TOP_K_RETRIEVE=15
TOP_K_RERANK=5
GUEST_MAX_CONCURRENT_REQUESTS=2
GUEST_MAX_QUEUE_SIZE=20
GUEST_REQUEST_TIMEOUT_SEC=45