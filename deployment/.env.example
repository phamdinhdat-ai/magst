# Database Configuration
# For PostgreSQL (production):
# DATABASE_URL=postgresql+asyncpg://username:password@localhost:5432/dbname

# For SQLite (development/testing) - leave DATABASE_URL empty or uncomment below:
# DATABASE_URL=sqlite+aiosqlite:///./guest_workflow.db

# SQLite database file path (only used if DATABASE_URL is not set)
SQLITE_DB_PATH=./guest_workflow.db

# Application Settings
GUEST_SESSION_EXPIRY_HOURS=24
GUEST_MAX_INTERACTIONS_PER_SESSION=100

# Vector Store Settings (if using Ollama embeddings)
# VECTOR_STORE_BASE_DIR=./vector_stores_data
# EMBEDDING_MODEL=nomic-embed-text
# OLLAMA_BASE_URL=http://localhost:11434
