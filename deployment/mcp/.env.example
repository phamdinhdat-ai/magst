# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=vectordb

# PgAdmin Configuration
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin_password_here

# MCP Server Configuration
MCP_PORT=50051
MCP_HOST=0.0.0.0
DEVICE=cpu

# AI Models Configuration
EMBEDDING_MODEL=AITeamVN/Vietnamese_Embedding_v2
RERANKING_MODEL=AITeamVN/Vietnamese_Reranker

# Database URL (auto-constructed from above values in docker-compose)
# DB_URL=postgresql+asyncpg://postgres:your_secure_password_here@db:5432/vectordb
